fastapi>=0.110.0
uvicorn>=0.27.0
gunicorn>=21.2.0
python-dotenv
requests
certifi
numpy
Pillow
onnxruntime            # CNN inference (replaces torch — ~5x less RAM)
firebase-admin
pydantic>=2.0.0

# LLM orchestration (LangGraph). Gemini + Sarvam are called via direct HTTP
# (requests) instead of the heavy langchain provider SDKs, to keep RAM low.
langgraph>=0.2.0
langchain-core>=0.3.0
